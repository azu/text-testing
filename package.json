{
  "private": true,
  "scripts": {
    "prepublish": "npm run bootstrap",
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test && mocha"
  },
  "devDependencies": {
    "lerna": "2.0.0-beta.24",
    "mocha": "^3.0.2",
    "text-testing-mocha": "^0.x.x"
  }
}
